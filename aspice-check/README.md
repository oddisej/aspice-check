# aspice-check

Orchestrator composing `confluence-ai` and `aspice-eval` into:
- `aspice-analyze` — full pipeline CLI (export → evaluate → publish)
- `aspice-mcp` — MCP server exposing evaluation and Confluence tools to AI agents

This is a skeleton README — full documentation is added in Task 18.3 of the
library-api-surface spec.
